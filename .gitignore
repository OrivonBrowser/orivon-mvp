# Dependencies
node_modules/

# Build output
dist/
out/
build/
*.tsbuildinfo

# Electron / packaging
release/
*.AppImage
*.deb

# Local agent + editor state
.claude/settings.local.json
.vscode/
.idea/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Spike-only build output and the isolated app tree (webtorrent is an app
# asset, never a shell dependency - see build-plan.md Platform policy)
spike/*/dist/
spike/app/node_modules/
spike/results/

# Autonomous build loop (docs/planning/autonomous-loop-design.md)
# State churns every cycle and is machine-owned, so it is not versioned.
orchestration/state/
orchestration/dashboard/.pid

# Credentials never enter this repository -- it is public. The loop reads them
# from ~/.config/orivon/notify.env (mode 600). scripts/check-no-secrets.mjs
# enforces this. Example files are allowed and are meant to be committed.
*.env
!*.env.example
